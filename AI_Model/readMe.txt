Don't run these python scripts without having a virtual env set up with requirements installed.

Will set up a requirements.txt file later that will allow for python's equivelent of "npm install" type functionality for easy dependancy install.